# qa-001 Run deterministic MVP scenario QA and create bug tickets

## Status

- status: done
- type: chore
- priority: 11
- owner agent: qa-agent
- branch: none
- commit message: none

## Goal

Run the PM-authored pre-AI MVP scenario test after reinforcement tasks are complete and document failed cases as bug tasks.

## Scope

- Test happy path from start to Normal Ending.
- Test wrong evidence penalty.
- Test oxygen loss condition.
- Test blackout behavior.
- Test recovered file flow.
- Test intro comprehension.
- Test internal file content readability.
- Test first-time player guidance and soft-lock prevention.
- Create `bug-###.md` files for failed cases.

## Dependencies

- before: `feat-003`, `feat-005`, `feat-006`, `feat-008`, `feat-010`, `chore-002`, `feat-011`
- after: bug fix tasks if failures are found

## Acceptance Criteria

- [x] QA results are documented in ops.
- [x] Each failure has clear reproduction steps.
- [x] Bug tasks include expected behavior, actual behavior, severity, and affected files if known.
- [x] QA explicitly treats external AI/API behavior as out of scope until `feat-007` is resumed.

## QA Environment

- date: 2026-08-05
- qa branch under test: `feat-011-improve-mvp-player-guidance-and-ux-guardrails`
- qa branch commit: `a2ca70e feat-011 merge latest main and resolve conflicts`
- scope baseline: deterministic pre-AI MVP after `feat-010`, `chore-002`, and `feat-011`
- external AI/API: out of scope

## Validation Commands

- `npm run build`: pass
- `git diff --check`: pass
- Vite dev server: started successfully at `http://127.0.0.1:5173/`
- HTTP smoke test: `curl -I http://127.0.0.1:5173/` returned `200 OK`
- Browser click automation: not run because Playwright/browser automation tooling is not installed in the current session

## Scenario Results

| scenario | result | notes |
| --- | --- | --- |
| Intro comprehension | pass | Multi-step intro explains player role, ECHO lockdown, suspected error, and Act sequence; intro remains skippable. |
| Internal file content readability | pass | File fixtures include richer clues, notes, and Act-relevant evidence context from `chore-002`. |
| First-time player guidance | pass | HUD/ECHO objective cards, file state legend, and attachment hints are visible in `feat-011`. |
| Happy path to Normal Ending A | pass by code path | Act evidence requirements and keyword intents map to Act 1 -> Act 2 -> Act 3 -> `ending-ready`; ending overlay exists. |
| Wrong evidence penalty | pass by code path | Wrong file set, unrecovered evidence, and missing explanation intent apply power penalty and return distinct feedback reasons. |
| Recovered file flow | pass by code path | Security password unlock and Log_Fixer recovery make `quarantine_rules.conf` eligible for Act 2 evidence. |
| Oxygen loss condition | fail | App never calls `advanceResourceTime`, so O2 does not decrease during gameplay. See `bug-001`. |
| Blackout behavior | fail | Blackout visual state exists, but interaction lock helper is not wired into UI controls. See `bug-002`. |

## Created Bug Tasks

- `bug-001`: Connect oxygen timer to active gameplay session
- `bug-002`: Enforce blackout interaction lock in gameplay UI

## Workflow Status Log

| date | agent | status change | notes |
| --- | --- | --- | --- |
| 2026-08-05 | qa-agent | todo -> in_progress | Deterministic pre-AI MVP QA started against feat-011 branch with latest main merged |
| 2026-08-05 | qa-agent | in_progress -> done | Build and server smoke passed; two gameplay resource failures documented as bug tasks |
