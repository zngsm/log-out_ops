# bug-012 Fix Log_Fixer Repair Button Disabled State on Second File Recovery

## Document Meta

- type: bug
- status: done
- owner: dev-agent
- priority: 61
- created: 2026-08-10
- completed: 2026-08-10

## User Report

Log_Fixer.exe 모달에서 첫 번째 파일(quarantine_rules.conf) 복구 완료 후, 두 번째 파일(휴지통 삭제 파일 등) 복구를 시도할 때 [복구 실행] 버튼이 영구적으로 비활성화(disabled)되는 버그 발생.

## Scope

- Update `Log_Fixer.exe` modal repair button disabled condition in `src/App.tsx`:
  - Replace global `isQuarantineRecovered` check with current target file recovery check (`isTargetFileAlreadyRecovered = targetFile ? recoveredFileIds.has(targetFile.id) : false`).
  - Ensure [RUN REPAIR] / [RECONSTRUCTING...] / [RECOVERED] button state reflects the selected target file.
- Update success highlight text dynamically (`${targetFile?.name ?? "File"} 복구 완료`).

## Acceptance Criteria

- Recovering a second file after `quarantine_rules.conf` allows the [RUN REPAIR] button to be active.
- Success highlight text dynamically displays the target file name upon successful recovery.
- Production build passes (`npm run build`).
- `git diff --check` passes with no issues.

## Dependencies

- Previous: `feat-017`, `feat-039`, `feat-043`
- Next: human review

## Implementation Notes

- Code commit: `bug-012 fix log fixer repair button disabled state when attempting second file recovery`
- Verification: `npm run build`, `git diff --check`

## Status Log

- todo: Log_Fixer repair button permanently disabled when attempting second file recovery after quarantine_rules.conf.
- in_progress: updated disabled check to use current target file recovery state and dynamic success message in App.tsx.
- done: build passed, reviewed, and ops tracking updated.

## Review History

- 2026-08-10: Reviewed code changes in `src/App.tsx` and verified acceptance criteria. `npm run build` and `git diff --check` passed cleanly. Status transition, target file recovery detection, dynamic success message, and repair button state handling verified. Decision: APPROVE.
