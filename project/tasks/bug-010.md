# bug-010 Fix ECHO Message Scroll And Clipping

## Document Meta

- type: bug
- status: done
- owner: dev-agent
- priority: 47
- created: 2026-08-09
- completed: 2026-08-09

## User Report

ECHO 메시지 영역이 자꾸 잘린다. 메시지 영역은 내부 스크롤이 가능해야 하고, ECHO 메시지가 아래로 계속 추가되므로 포커스도 신규 메시지로 맞춰져야 한다.

## Scope

- Replace last-element `scrollIntoView` behavior with direct message container scrolling.
- Ensure the message history panel owns its internal vertical scroll.
- Prevent long message text from clipping outside bubbles.
- Cap the composer height so input/feedback controls cannot consume the whole ECHO panel.
- Preserve fixed terminal screen layout without page scrolling.

## Acceptance Criteria

- ECHO chat history scrolls internally inside the right panel.
- New messages automatically scroll the history to the latest entry.
- Long player/ECHO/system messages wrap instead of being clipped.
- Composer can scroll internally if feedback text grows.
- Production build passes.

## Dependencies

- Previous: `feat-030`, `feat-031`
- Next: human visual/play review

## Implementation Notes

- Code commit: `bug-010 fix echo message scroll clipping`
- Verification: `npm run build`, `git diff --check`

## Status Log

- todo: user reported clipped ECHO messages and missing latest-message focus.
- in_progress: message container ref, direct scrollTop control, and CSS overflow fixes applied.
- done: build passed and ops tracking updated.
