# bug-011 Fix Workstation Viewport Focus Drift

## Document Meta

- type: bug
- status: done
- owner: dev-agent
- priority: 51
- created: 2026-08-09
- completed: 2026-08-09

## User Report

전체적으로 화면 포커스가 아래로 맞춰지면서 일반 게임 화면에서 상단이 잘린다. 워크스테이션 화면에서 ECHO 메시지가 아래로 추가될 때 전체 화면이 내려가면 안 되고, 내부 채팅 영역만 최신 메시지로 이동해야 한다.

## Scope

- Replace workstation ECHO `scrollIntoView` behavior with direct internal chat container scrolling.
- Keep the workstation screen fixed to the viewport top.
- Ensure work split body, left report panel, and right ECHO panel have `min-height: 0` and internal overflow constraints.
- Prevent chat messages from forcing the full page or monitor surface downward.
- Preserve ECHO latest-message focus inside the chat container.

## Acceptance Criteria

- Workstation header remains visible at the top of the screen.
- Full game viewport does not scroll down when ECHO messages append.
- Right ECHO chat panel scrolls internally to the newest message.
- Left report panel scrolls internally without moving the entire viewport.
- Production build passes.

## Dependencies

- Previous: `bug-010`, `feat-034`
- Next: human visual review

## Implementation Notes

- Code commit: `bug-011 fix workstation viewport focus drift`
- Verification: `npm run build`, `git diff --check`

## Status Log

- todo: user reported viewport focus is shifted downward, clipping the top of the game.
- in_progress: WorkInterface chat scroll ref and workstation layout overflow constraints updated.
- done: build passed and ops tracking updated.
